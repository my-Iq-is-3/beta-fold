# Find wheels for PyTorch Geometric compatible with PyTorch 1.10.* and CUDA 11.3
-f https://data.pyg.org/whl/torch-1.10.0+cu113.html

# Core PyG dependencies (specify versions if known issues arise)
torch-scatter
torch-sparse
torch-cluster
# torch-spline-conv # Only if you specifically need it

# Main PyG library
torch-geometric

# Other dependencies
e3nn==0.3.3
wandb==0.12.0
pynvml==11.0.0
git+https://github.com/NVIDIA/dllogger#egg=dllogger
einops
bitsandbytes # Install without version first, may need pinning later if build fails
rna-fm # Assuming this is compatible
networkx
torchdata==0.4.0 # Use older version compatible with PyTorch ~1.10