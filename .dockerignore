# Git history
.git
# Virtual environments
.venv
venv
env
*.conda*
# Python cache/build files
__pycache__/
*.pyc
*.pyo
*.pyd
build/
dist/
*.egg-info/
# IDE files
.vscode/
.idea/
# Datasets / Large files (add your specific ones)
data/
models/
*.pth
*.pt
*.ckpt
# Other build artifacts
dgl_build_temp/ # If it gets created locally outside Docker
# OS specific
.DS_Store
Thumbs.db
proj/
.ipynb_checkpoints/
# --- IMPORTANT ---
# Now, explicitly UN-ignore the files/folders needed for the build

# Allow the Dockerfile itself (doesn't really affect context, but good practice)
!Dockerfile

# Allow the requirements file needed by the Dockerfile
!requirements.txt